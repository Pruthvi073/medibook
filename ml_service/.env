# MediBook ML Service — Environment Variables

# Flask server port (must match ML_SERVICE_URL in backend .env)
PORT=5001

# Path to the trained model bundle (relative to ml_service/)
MODEL_PATH=model/model.pkl

# Set to 1 to enable Flask debug mode (auto-reload on code changes)
# Keep at 0 in production
FLASK_DEBUG=0
